import { Rate } from 'src/app/shared/models/rate.model';
import { PERIODS } from './periods.mock';

export const RATES: Rate[] = [
    { name: 'Hungary', code: 'HU', countryCode: 'hu', periods: PERIODS}
];
